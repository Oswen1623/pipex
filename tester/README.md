# Tester Pipex

Utilisation:

```
./test.sh mandatory   # tests obligatoires
./test.sh bonus       # tests bonus
./test.sh all         # tous les tests
```

Pré-requis:
- `make` disponible
- exécutable `./pipex` construit dans ce dossier
